{
  "name": "tunetogether-frontend",
  "version": "2.0.0",
  "description": "TuneTogether frontend (static HTML/CSS/JS) - talks to the Java backend over REST + WebSocket/STOMP instead of Firebase",
  "scripts": {
    "start": "python -m http.server 8080",
    "serve": "npx http-server -p 8080"
  },
  "license": "MIT"
}
